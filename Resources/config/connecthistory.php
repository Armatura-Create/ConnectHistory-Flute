<?php

declare(strict_types=1);

return [
    /*
     * Сколько секунд держать в кеше результаты запросов.
     * Метрики и графики считаются агрегатами и меняются медленно;
     * онлайн обновляется часто, поэтому у него отдельное короткое окно.
     */
    'cache' => [
        'metrics' => 60,
        'charts' => 300,
        'online' => 15,
        'identity' => 900,
    ],

    /*
     * Верхняя граница окна выборки в днях. Защищает от запроса «дай всё за пять лет»:
     * стоимость страницы не должна зависеть от того, сколько истории накопилось.
     */
    'max_period_days' => 365,

    'default_period_days' => 7,

    /*
     * Сессии короче этого числа секунд считаются «заглянул и вышел».
     * Фильтр в интерфейсе позволяет их отбросить, чтобы статистика посещаемости
     * не состояла из случайных коннектов.
     */
    'short_session_seconds' => 60,

    /*
     * Сколько строк показывать на странице в таблицах по умолчанию.
     */
    'per_page' => 25,

    /*
     * Потолок числа групп в сводном режиме экрана «Сессии».
     *
     * Cycle SelectQuery::count() игнорирует GROUP BY, поэтому пагинировать свод
     * силами платформы нельзя — агрегат приезжает в PHP целиком. Потолок делает
     * расход памяти зависящим от числа ГРУПП, а не от объёма истории:
     * миллион сессий, свёрнутых в 500 строк, стоит столько же, сколько тысяча.
     */
    'max_groups' => 500,

    /*
     * Плагин снимает точку онлайна раз в OnlineSnapshotIntervalSeconds (по умолчанию 300).
     * Разрыв больше указанного числа секунд трактуется как «сервер молчал».
     */
    'snapshot_gap_seconds' => 900,
];
